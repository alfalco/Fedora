# Add Negativo17 repo to system
sudo dnf config-manager --add-repo=https://negativo17.org/repos/fedora-spotify.repo

# Install package
sudo dnf install spotify-client

# HiDPI

# Edit /usr/share/applications/spotify.desktop and add --force-device-scale-factor=1.5 between spotify and %U on Exec= line. Change order with TryExec, add options to this or comment line.
